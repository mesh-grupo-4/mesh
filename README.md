# Mesh

Proyecto de tesis — coordinación grupal en tiempo real para actividades outdoor.

## Backend

Ver [`backend/README.md`](backend/README.md).

**Migraciones DB (Supabase):** usar `npx prisma migrate deploy`, no `npm run db:migrate`. Ver sección *Migraciones con Supabase + Prisma* en el README del backend.

## Frontend

Ver [`frontend/README.md`](frontend/README.md).
