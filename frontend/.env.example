# Modo LAN (recomendado): NO definir — auto-detect desde Metro (misma WiFi que la PC).
# EXPO_PUBLIC_API_URL=

# Modo túnel (UTN / AP isolation): URL HTTPS de ngrok apuntando al puerto 3000.
# EXPO_PUBLIC_API_URL=https://xxxx.ngrok-free.app

# Emulador Android (solo si auto-detect falla):
# EXPO_PUBLIC_API_URL=http://10.0.2.2:3000
